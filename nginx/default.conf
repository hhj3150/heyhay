# HEY HAY MILK ERP — Nginx 설정
# HTTP → HTTPS 리다이렉트 + API 프록시 + SPA

# HTTP (인증서 발급용 + 리다이렉트)
server {
    listen 80;
    server_name _;

    # Let's Encrypt 인증서 발급/갱신용
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 나머지는 프론트엔드 서빙 (SSL 없을 때 fallback)
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 256;

    # API 프록시
    location /api/ {
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
    }

    # 정적 파일 캐시
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA 라우팅
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS (SSL 인증서 생성 후 활성화)
# server {
#     listen 443 ssl http2;
#     server_name erp.heyhay.kr;
#
#     ssl_certificate /etc/letsencrypt/live/erp.heyhay.kr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/erp.heyhay.kr/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml;
#     gzip_min_length 256;
#
#     location /api/ {
#         proxy_pass http://backend:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#         try_files $uri =404;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
